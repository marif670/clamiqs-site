name = "calmiqs-site"
compatibility_date = "2025-11-02"
pages_build_output_dir = "dist"

[observability]
enabled = true

# Environment variables (non-sensitive)
[vars]
BLOG_FILE = "posts.json"

# DO NOT add secrets here. Manage secrets via Cloudflare Dashboard.
